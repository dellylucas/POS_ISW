package ConexionBD;

import java.sql.SQLException;
import java.sql.Statement;



public class DAO_Eliminar {
	
	/*public DAO_Eliminar(){
		
	}
	
	public void eliminarMedicoDAO(Medicos medico) {
		Conexion conex = new Conexion();
		try {
			Statement estatuto = conex.getConnection().createStatement();
			estatuto.executeUpdate("DELETE from medicos  WHERE NombreMedico= '" + 
			medico.getNombreMedico()+"'");
			conex.desconectar();

		} catch (SQLException e1) {
			
		}
	}
	
	public void eliminarEspecialidadDAO(Medicos medico) {
		Conexion conex = new Conexion();
		try {
			Statement estatuto = conex.getConnection().createStatement();
			estatuto.executeUpdate("DELETE from especialidad  WHERE IdentificacionMedico= '" + 
			medico.getIdentificacion()+"'");
			conex.desconectar();

		} catch (SQLException e1) {
			
		}
	}
	
	public void eliminarDisponibilidadDAO(Medicos medico) {
		Conexion conex = new Conexion();
		try {
			Statement estatuto = conex.getConnection().createStatement();
			estatuto.executeUpdate("DELETE from disponibilidad WHERE IdentificacionMedico= '" + 
			medico.getIdentificacion()+"'");
			conex.desconectar();

		} catch (SQLException e1) {
			
		}
	}
	
	public void eliminarServicioDAO(Servicios servicio) {
		Conexion conex = new Conexion();
		try {
			Statement estatuto = conex.getConnection().createStatement();
			estatuto.executeUpdate("DELETE from servicios WHERE NombreServicio= '" + 
			servicio.getNombreServicio()+"'");
			conex.desconectar();

		} catch (SQLException e1) {
			
		}
	}*/
}
